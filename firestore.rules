rules_version = '2';
// USO LOCAL/EMULADOR SOMENTE — NUNCA rodar firebase deploy com este arquivo.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
